---
interface Props {
  t: Record<string, any>;
}

const { t } = Astro.props;
---

<section class="section" id="evidence">
  <h2>{t.evidence.title}</h2>
  <p class="section-intro">{t.evidence.intro}</p>
  <ul class="evidence-list">
    {t.evidence.items.map((item: string) => (
      <li>{item}</li>
    ))}
  </ul>
</section>
